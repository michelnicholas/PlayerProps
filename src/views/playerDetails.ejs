<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= player.name %> Details</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="bg-gray-100 dark:bg-gray-900 text-center">
    <div class="container mx-auto mt-5">
      <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-200 mb-5">
        <%= player.name %> Details
      </h2>
      <div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-lg">
        <img
          src="<%= player.image %>"
          alt="<%= player.name %>"
          class="w-32 h-32 mx-auto rounded-full mb-4"
        />
        <h3 class="text-xl font-bold text-gray-900 dark:text-gray-100">
          <%= player.name %>
        </h3>
        <p class="text-gray-700 dark:text-gray-300">Team: <%= player.team %></p>
        <p class="text-gray-700 dark:text-gray-300">
          Points per Game: <%= player.points %>
        </p>
        <!-- Add more player details as needed -->
      </div>
      <a
        href="/players"
        class="mt-3 inline-block bg-blue-500 dark:bg-blue-700 text-white py-2 px-4 rounded"
        >Back to Players</a
      >
    </div>
  </body>
</html>
